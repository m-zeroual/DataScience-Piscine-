[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "A sample test package"
readme = "README.md"
authors = [
    {name = "mzeroual", email = "mzeroual@42Angouleme.fr"}
]
license = {text = "MIT"}
requires-python = ">=3.7"